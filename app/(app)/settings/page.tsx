import { PageHeader } from '@/components/page-header';

export default function SettingsPage() {
  return (
    <div>
      <PageHeader title="Settings" subtitle="Owner account configuration and preferences." />
      <div className="card p-4 text-sm text-slate-600">Phase 1 settings placeholder: profile, defaults, and notification preferences.</div>
    </div>
  );
}
